<div>
    <h2 class="mb-2">Organisers</h2>

    <ul class="flex flex-wrap list-reset">
        {% for organiser in info.organisers %}
            {% include 'partials/organiser.html.twig' %}
        {% endfor %}
    </ul>
</div>
