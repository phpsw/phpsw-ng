#
# {{ ansible_managed }}
#

server {
    listen 80;
    listen [::]:80;
    server_name phpsw.local;

    keepalive_timeout 60;

    root /vagrant/web;
    index index.html;

    location / {
        try_files $uri $uri.html index.html;
    }
}
